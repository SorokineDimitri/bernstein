apiVersion: v1
kind: Service
metadata:
  name: cadvisor-service
  namespace: kube-system
  labels:
    app: cadvisor
spec:
  selector:
    app: cadvisor
  ports:
    - port: 8080
      targetPort: 8080
